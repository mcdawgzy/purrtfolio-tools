# Trading Tools by Purrtfolio — web dashboard
# FastAPI: fastapi + uvicorn
# SQLite comes with Python stdlib (no need to install)
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pandas>=2.0.0
yfinance>=0.2.0
